@page
@model PhamHongPhucRazorPages.Pages.News.PublicIndexModel
@{
    ViewData["Title"] = "Public News";
}

<div class="container-fluid">
    <div class="d-flex flex-wrap justify-content-between align-items-end gap-3 mb-4">
        <div>
            <h1 class="page-title">FUNews</h1>
            <p class="text-muted mb-0">Latest active news for public readers.</p>
        </div>
        <form class="d-flex gap-2" method="get">
            <input class="form-control" asp-for="Search" placeholder="Search news..." />
            <button class="btn btn-primary"><i class="bi bi-search"></i></button>
        </form>
    </div>
    <div class="row g-4">
        @foreach (var article in Model.Articles)
        {
            <div class="col-md-6 col-xl-4">
                <article class="card news-card">
                    <div class="card-body">
                        <span class="badge text-bg-success mb-3">@article.Category?.CategoryName</span>
                        <h5>@article.NewsTitle</h5>
                        <p class="text-muted">@article.Headline</p>
                        <p>@article.NewsContent</p>
                        <div class="small text-muted">@article.CreatedDate.ToString("dd/MM/yyyy") - @article.CreatedBy?.AccountName</div>
                    </div>
                </article>
            </div>
        }
    </div>
</div>
